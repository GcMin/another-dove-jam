# AGENTS.md

本文件约束在本仓库中工作的 AI Agent、Codex、自动化开发工具及人工协作者。

## 仓库模式

本仓库不是传统的“所有人共同修改同一个游戏主分支”。

- `main` = 共同规则、模板、跨分支决策。
- `proposal/<developer>/<game-slug>` = 某位开发者独立负责的一套游戏方案。

## Agent 在 Main 上必须遵守

当当前分支为 `main` 时：

1. 不创建具体游戏的 `project.godot`、场景、脚本、资源或玩法实现。
2. 不替任何 proposal 设计角色、Boss、关卡、技能、剧情或数值并写入 main。
3. 只允许维护协作规则、模板、通用 Git 配置和已经确认的跨分支决策。
4. 不把未经确认的“最佳实践”自动升级成所有 proposal 的强制规则。
5. 不直接把 proposal 的代码复制进 main。

如果任务明显属于具体游戏方案，而当前仍在 `main`，应先切换到或创建对应的 proposal 分支，再修改仓库。

## Agent 在 Proposal 分支上必须遵守

1. 只围绕该分支的一款游戏进行设计与实现。
2. 优先阅读该分支的 `README.md`、`docs/GAME_DESIGN.md`、`docs/DECISIONS.md`。
3. 用户的新明确需求覆盖旧设计，但必须同步更新相关文档，避免代码和策划各自生活在不同宇宙。
4. 不主动修改其他开发者的 proposal 分支。
5. 不把本分支的玩法或架构写回 main，除非用户明确要求形成跨分支规则。
6. 不因“可能以后需要”而提前建设大型公共框架。
7. 原型阶段优先验证核心玩法，不用无关的安全边界测试、基础设施或过度抽象拖慢可玩验证。

## 变更纪律

- 修改代码时同步维护与行为直接相关的设计/运行说明。
- 关键玩法取舍记录到本 proposal 的 `docs/DECISIONS.md`。
- 任何无法从代码直接推导、但会影响下一位开发者继续工作的约束，都应文档化。
- 不删除其他人的设计记录来“清理仓库”。被推翻的重大决策应保留历史并注明 superseded。

## PR 到 Main

只有真正跨 proposal 的内容才应提交到 `main`。

PR 必须回答：

- 为什么这不是单一 proposal 的事情？
- 对现有 proposal 有什么影响？
- 是否需要迁移？
- 是否改变 main 的职责？

如果答不出来，改动通常应该留在 proposal 分支。